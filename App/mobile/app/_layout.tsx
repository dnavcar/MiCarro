import React from 'react';
import ModalApp from './modal';

export default function RootLayout() {
  // Render the modal (app) directly in the root layout as requested.
  return (
    <ModalApp />
  );
}
